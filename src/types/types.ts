export type LoginFormValues = {
  email: string;
  password: string;
};

export type Action = {
  id: string;
  name: string;
  description: string;
  icon: string;
  color: string;
  status: number;
  createdAt: string;
};
